document.addEventListener('click',async e=>{const b=e.target.closest('[data-copy-prompt]');if(!b)return;const text=document.getElementById('example-prompt').innerText;const status=document.querySelector('.copy-status');try{await navigator.clipboard.writeText(text);status.textContent=b.dataset.copied;}catch{const r=document.createRange();r.selectNodeContents(document.getElementById('example-prompt'));const s=getSelection();s.removeAllRanges();s.addRange(r);status.textContent='';}});
